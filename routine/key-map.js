// const fs = require("fs");
// const { resolve: PathResolve } = require("path");
// const configPath = PathResolve(PathResolve("./"), "config")

const keyMap = {
  processKey: "_WK_SERVER_CONFIG",
};

module.exports = keyMap;
